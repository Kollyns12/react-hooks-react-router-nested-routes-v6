// The main App component — this serves as the root component of the React application.
// Currently, it only contains an empty <header> element inside a React fragment.
function App() {
  return (
    <>
      <header>
        {/* You can add navigation links, a logo, or a title here later */}
      </header>
    </>
  );
};

export default App;

